extends Spatial
var propriedade = "nenhuma"
var cor = "RED"

func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	pass

func adicionarprop(valor):
	
	propriedade = valor